<!--<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header"><%= t('message.Listando', model: @placa_carros.model_name.human(count: 4)) %></h1>
    </div>
</div>

<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
        <th>Placa do carro</th>
        <th>Proprietário</th>
        <th>Modelo</th>
        <th>Ano</th>
        <th>
          <%= link_to new_placa_carro_path, class:"btn btn-success btn-circle" do %>
            <i class="fa fa-plus"></i>
          <% end %>
        </th>
      </tr>
    </thead>
    <tbody>
          <% @placa_carros.each do |placa_carro| %>
          <tr>
              <td><%= placa_carro.placa %></td>
              <td><%= placa_carro.proprietario %></td>
              <td><%= placa_carro.modelo %></td>
              <td><%= placa_carro.ano %></td>
              <td style="width: 100px">

               

                <%= link_to edit_placa_carro_path(placa_carro), class:"btn btn-primary btn-circle" do %>
                  <i class="fa-edit"></i>
                <% end %>

                <%= link_to placa_carro_path(placa_carro), method: :delete, class:"btn btn-danger btn-circle", data: { confirm: t('message.confirm_with', item: placa_carro.id ) }  do %>
                  <i class="icons"></i>
                <% end %>
              </td>
          </tr>
          <% end %>
      </tbody>
  </table>
</div>-->
<p id="notice"><%= notice %></p>

<h1><%= PlacaCarro.model_name.human(count: @placa_carros.count) %></h1>
<table class="table table-hover">
  <thead>
    <tr>
      <th><%= @placa_carros.human_attribute_name(:Placa)%></th>
      <th><%= @placa_carros.human_attribute_name(:proprietario) %></th>
      <th><%= @placa_carros.human_attribute_name(:modelo) %></th>
      <th><%= @placa_carros.human_attribute_name(:ano) %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @placa_carros.each do |placa_carro| %>
      <tr>
        <td><%= placa_carro.placa %></td>
        <td><%= placa_carro.proprietario %></td>
        <td><%= placa_carro.modelo %></td>
        <td><%= placa_carro.ano %></td>
        <td><%= link_to t('links.Editar'), edit_placa_carro_path(placa_carro) %></td>
        <td><%= link_to t('links.Deletar'), placa_carro, method: :delete, data: { confirm: t('messages.confirm') } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to t('new.criar_novo_registro'), new_placa_carro_path, class:"btn btn-primary" %>